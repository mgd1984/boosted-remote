# Mechanical Scripts

This folder keeps the active mechanical workflow entry points at the top level.

## Active Utilities

- `build_enclosure.py`
  - Main build123d enclosure generator.
- `analyze_proxy_glb.py`
  - Samples proxy cross-sections for enclosure fitting.

Older reconstruction and part-study scripts were moved to
`history/mechanical/scripts/`.

Use `mechanical/cad/` with the top-level scripts here for current enclosure
work.